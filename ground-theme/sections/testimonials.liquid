{%- comment -%}
  GROUND - Testimonials Section
  Features: Customer testimonials in glass cards
{%- endcomment -%}

<section class="section" data-section-id="{{ section.id }}">
  <div class="container">
    <div class="section-header" data-animate>
      {%- if section.settings.overline != blank -%}
        <p class="overline">{{ section.settings.overline }}</p>
      {%- endif -%}

      <h2 class="heading-xl">{{ section.settings.heading | default: 'What People Are Saying' }}</h2>

      {%- if section.settings.description != blank -%}
        <p>{{ section.settings.description }}</p>
      {%- endif -%}
    </div>

    <div class="testimonials-grid">
      {%- for block in section.blocks -%}
        <div class="testimonial-card glass-card" data-animate style="animation-delay: {{ forloop.index | times: 100 }}ms;">
          <div class="testimonial-rating">
            {%- for i in (1..5) -%}
              <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
              </svg>
            {%- endfor -%}
          </div>

          <p class="testimonial-content">"{{ block.settings.quote }}"</p>

          <div class="testimonial-author">
            <div class="testimonial-avatar">
              {%- if block.settings.avatar != blank -%}
                <img
                  src="{{ block.settings.avatar | image_url: width: 96 }}"
                  alt="{{ block.settings.name }}"
                  width="48"
                  height="48"
                  loading="lazy"
                >
              {%- else -%}
                {{ block.settings.name | slice: 0 }}
              {%- endif -%}
            </div>
            <div class="testimonial-info">
              <h4>{{ block.settings.name }}</h4>
              <span>{{ block.settings.title }}</span>
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-top: 4rem;
  }

  .testimonial-card {
    padding: 2rem;
    display: flex;
    flex-direction: column;
  }

  .testimonial-rating {
    display: flex;
    gap: 0.25rem;
    color: #fbbf24;
    margin-bottom: 1.25rem;
  }

  .testimonial-content {
    font-size: 1rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    flex: 1;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(0, 0, 0, 0.06);
  }

  .testimonial-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--gradient-mid), var(--gradient-end));
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 1.125rem;
    color: var(--accent-primary);
    overflow: hidden;
  }

  .testimonial-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .testimonial-info h4 {
    font-size: 0.9375rem;
    font-weight: 600;
    margin-bottom: 0.125rem;
    color: var(--text-primary);
  }

  .testimonial-info span {
    font-size: 0.8125rem;
    color: var(--text-muted);
  }

  @media (max-width: 1024px) {
    .testimonials-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .testimonials-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

{% schema %}
{
  "name": "Testimonials",
  "tag": "section",
  "class": "testimonials-section",
  "settings": [
    {
      "type": "text",
      "id": "overline",
      "label": "Overline Text",
      "default": "Testimonials"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "What People Are Saying"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "Ground has changed how I study forever. I'm much more efficient and I don't stress about deadlines anymore."
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Adam"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Student"
        },
        {
          "type": "image_picker",
          "id": "avatar",
          "label": "Avatar"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "quote": "As an ADHD coach, I highly recommend the Ground device to my high school and college clients. This tool has been transformative in helping them manage distractions and enhance their productivity.",
            "name": "Dr. Paula",
            "title": "Lotus Coaching Services"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "Ground has changed how I study forever. I'm much more efficient and I don't stress about deadlines anymore. I'm finally able to study without getting distracted.",
            "name": "Adam",
            "title": "Student"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "Since I've been using Ground for our household, I can block TikTok and Instagram while they're at school, and keep important apps like Call and Messages.",
            "name": "Jen",
            "title": "Parent"
          }
        }
      ]
    }
  ]
}
{% endschema %}
